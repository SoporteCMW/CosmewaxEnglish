# Segunda barrera, por si el .htaccess raíz no se aplica (AllowOverride None, etc.)
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
